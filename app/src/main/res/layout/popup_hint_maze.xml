<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    android:id="@+id/hintContainer">

    <LinearLayout
        android:id="@+id/popupBox"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_centerInParent="true"
        android:background="@drawable/bg_box_sand"
        android:gravity="center"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineHeight="27dp"
            android:padding="20dp"
            android:text="The secrets lie where your journey began. Unravel the hieroglyphics etched in stone. They hold the key to the true path out."
            android:textAlignment="center"
            android:textColor="@color/sand" />
    </LinearLayout>


    <TextView
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/popupBox"
        android:layout_centerHorizontal="true"
        android:background="@drawable/bg_box_sandoutline"
        android:padding="15dp"
        android:text="Hint"
        android:textAlignment="center"
        android:textColor="@color/dark_bg"
        android:textStyle="bold"
        android:translationY="30dp"
        app:layout_constraintBottom_toTopOf="@+id/popupBox"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <ImageView
        android:id="@+id/closeHintBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/popupBox"
        android:layout_centerHorizontal="true"
        android:background="@drawable/bg_box_sandoutline"
        android:padding="5dp"
        android:src="@drawable/close_icon"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/popupBox"
        app:layout_constraintVertical_bias="0.2" />


</androidx.constraintlayout.widget.ConstraintLayout>